from setuptools import setup, find_packages

setup(
    url='https://github.com/teamnightsky/sockbyte',
    name='sockbyte',
    author='TeamNightSky',
    license='LGPL',
    packages=find_packages(include=["sockbyte"]),
)
